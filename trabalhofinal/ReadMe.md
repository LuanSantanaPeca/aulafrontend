O sistema utiliza Node.js para possibilitar a edição do arquivo JSON externo em tempo real.

Para o funcionamento correto, após baixar o repositório, rode 'npm install' na raíz do diretório (caminho\caminho\trabalhofinal). Também na raíz do diretório rode 'npm run start' e abra 